# 安全说明

## 报告安全问题

请优先使用 GitHub 仓库的 Private vulnerability reporting 功能报告安全问题。不要在公开 Issue 中粘贴 `auth.json`、Cookie、API Key、访问令牌、账号邮箱或未经处理的接口响应。

报告中可以包含：

- 受影响的版本或提交。
- 可复现问题的最小步骤。
- 已脱敏的日志或示例数据。
- 可能的影响和修复建议。

## 本地敏感数据

本项目会在本机读取或保存以下敏感数据：

- `~/.codex/auth.json`
- `~/.hua-quota/accounts/`
- 用户自行创建的 `.env` 和 Cookie 文件

这些文件不会被程序主动上传，但拥有文件访问权限的本地进程仍可能读取它们。请保持操作系统和本地账号安全，不要把数据目录同步到公开网盘或代码仓库。

## 支持范围

只对当前默认分支提供安全修复。Codex/ChatGPT 网页端额度接口并非公开 API；接口变化导致的兼容性问题通常作为普通缺陷处理，而不是安全漏洞。
